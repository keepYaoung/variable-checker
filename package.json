{
  "name": "variable-token-checker",
  "version": "0.1.0",
  "private": true,
  "description": "Compare two screens' design tokens (Figma plugin).",
  "license": "SEE LICENSE IN LICENSE",
  "author": "keepYaoung <vbdd556@gmail.com>",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "build:test": "esbuild src/compare.ts --bundle --format=esm --outfile=test/compare.bundle.mjs",
    "test": "npm run build:test && node --test test/compare.test.mjs"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.108.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
